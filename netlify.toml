[build]
  base = "packages/web"
  command = "cd ../.. && pnpm install && pnpm build:core && cd packages/web && npx nuxi build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-optional"
  NITRO_PRESET = "netlify"
