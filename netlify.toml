[build]
  command = "pnpm install && pnpm build:core && cd packages/web && npx nuxt build"
  publish = "packages/web/dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-optional"
  NITRO_PRESET = "netlify"
